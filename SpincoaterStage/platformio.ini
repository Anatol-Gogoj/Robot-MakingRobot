[env:nanorp2040connect]
platform = raspberrypi
board = nanorp2040connect
framework = arduino
monitor_speed = 115200
; ODriveUART library removed in v2.6 — it uses property paths
; (axis0.encoder.vel_estimate, etc.) that return "invalid property"
; on ODrive S1, causing serial timeouts. All comms are now raw ASCII.
lib_deps =
